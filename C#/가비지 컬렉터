C#에 내장되어 있는 가비지 컬렉터에 대해서 알아볼 것이다.
주소 : https://docs.microsoft.com/en-us/dotnet/standard/garbage-collection/

닷넷 가비지 컬렉터는 메모리의 할당 및 해제를 담당한다. 객체를 만들때마다 CLR은 매니지드 힙에서부터 객체를 위한 메모리를 할당시키려고 하며,
매니지드 힙에서 주소 공간이 사용 가능한 만큼은 런타임은 계속해서 새 객체를 위해 공간을 할당시킨다.

그러나 메모리는 무한하지 않다. 결국 가비지 컬렉터는 여유 메모리 공간을 위해 컬렉션을 수행한다.
